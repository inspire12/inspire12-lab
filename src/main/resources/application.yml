

spring:
  session:
  store-type: jdbc
  jdbc.initialize-schema: always
  main:
    allow-bean-definition-overriding: true

logging:
  level:
    org.springframework.cache: TRACE

management:
  endpoints:
    web:
      exposure:
        include:
          - "info"
          - "health"
          - "httptrace"